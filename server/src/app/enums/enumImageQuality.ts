/**
 * Enum que indica la calidad de la imagen desplegada de las cartas
 * 
 * @LOW indica que la carta se puede desplegar en baja calidad
 * @HIGH indica que la carta se puede desplegar en alta calidad
 */
export enum EnumQuality {
  LOW = 'low',
  HIGH = 'high',
}